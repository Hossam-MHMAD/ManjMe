[project]
name = "ManjMe"
version = "0.1.0"
description = "English Teacher Dashboard"
readme = "README.md"
authors = [
    { name = "Hossam-MHMAD", email = "hossamtulimat2009@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
kamar-project = "kamar_project:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
